name = "customer-a"
domain = "example.com"
sending_ip = "192.0.2.20"
mail_from = "test@example.com"
recipient = "sink@example.net"
selector = "halon"
tags = ["lab", "implementation"]
# Password values are never stored here.
# username = "test-user"
# password_env = "MAILBENCH_SMTP_PASSWORD"

[smtp]
host = "smtp.example.com"
port = 25
ehlo = "mail.example.com"
tls = "starttls"
no_verify = false
timeout_secs = 10
